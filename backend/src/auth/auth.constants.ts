/** Nome do cookie HttpOnly que carrega o refresh_token. */
export const REFRESH_COOKIE_NAME = 'refresh_token';

/** Caminho ao qual o cookie de refresh fica restrito. */
export const REFRESH_COOKIE_PATH = '/auth';
