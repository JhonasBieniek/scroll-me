// Gerado por scripts/sync-env.mjs — não edite manualmente.
// Fonte: API_BASE_URL em .env (raiz do projeto) ou variável de ambiente no build.

export const environment = {
  apiBaseUrl: 'http://localhost:3000',
} as const;
